<?xml version="1.0" encoding="utf-8"?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:SaveUp.Views"
       x:Class="SaveUp.AppShell">

    <ShellContent Title="SaveUp" ContentTemplate="{DataTemplate views:StartPage}" />
    <ShellContent Title="Add New Entry" ContentTemplate="{DataTemplate views:EntryPage}" Route="EntryPage" />
</Shell>
